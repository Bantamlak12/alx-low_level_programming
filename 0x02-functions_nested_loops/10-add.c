#include "main.h"
/**
 * add - add two integers and return result
 * @x: a number to be add
 * @y: a number to be add
 * Return: Return the result
 */
int add(int x, int y)
{
	int result;

	result = x + y;

return (result);
}
