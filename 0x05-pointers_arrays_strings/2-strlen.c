#include "main.h"
#include <string.h>
/**
 * _strlen - a function that returns the length of a string
 * @s: length of a string
 * Return: length of string
 */
int _strlen(char *s)
{
	int length = 0;

	length = strlen(s);

	return (length);
}
