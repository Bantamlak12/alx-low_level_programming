#include <stdio.h>
#include <stdlib.h>
/**
 * main - a program that prints its name
 * @argv: a pointer array that points to argument passed
 * @argc: number of arguments passed
 * Return: Always 0.
 */
int main(int argc __attribute__((unused)), char *argv[])
{
	int i = 0;

	printf("%s\n", argv[i]);
	return (0);
}
